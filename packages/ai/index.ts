import { google } from "@ai-sdk/google";
import { generateText } from "ai";

export class AIService {
  constructor() {}

  init = async () => {
  }

  generateGeneralResponse = async (input: string): Promise<string> => {

  }
}
